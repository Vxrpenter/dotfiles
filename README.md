# Installation
For installation just run this command or download the script in the `/linux/` dir

**Codeberg Source:**
```sh
sudo sh -c "$(curl -fsSL https://codeberg.org/Vxrpenter/dotfiles/raw/branch/main/linux/setup-arch.sh)"
```

**GitHub Source:**
```sh
sudo sh -c "$(curl -fsSL https://raw.githubusercontent.com/Vxrpenter/dotfiles/main/linux/setup-arch.sh)"
```

# Includes
- Alacritty
- Zsh configs
- Fastfetch configs
- Program Setup
- Usefull packages
